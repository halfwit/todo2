package main 

const (
	errNoArg = "missing/invalid argument" 
	errNoCmd = "missing/invalid command"
        errBadArg = "unexpected argument supplied"
	errBadTodo = "malformed .todo file"
	errExists = "entry already exists"
	errNoEntry = "no such entry"
	errNoTodo = "no .todo file found"
	errNoTask = "missing/invalid taks"
)
